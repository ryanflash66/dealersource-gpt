{
  "name": "dealersource-gpt",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {"node": ">=24.0.0"},
  "scripts": {
    "test": "node scripts/test.ts",
    "pipeline": "node src/cli.ts",
    "dashboard:dev": "node scripts/dashboard.ts",
    "dashboard:build": "node scripts/build.ts",
    "deploy": "npx vercel --prod",
    "db:test": "node scripts/db-test.ts",
    "acceptance": "node scripts/acceptance.ts"
  },
  "dependencies": {},
  "devDependencies": {}
}
